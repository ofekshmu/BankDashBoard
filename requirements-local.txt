# Full dependencies for local CLI use (Excel parsing, charts, etc.)
# Install with:  pip install -r requirements-local.txt
-r requirements.txt
matplotlib>=3.7.0
seaborn>=0.12.0
openpyxl>=3.1.0
xlwings>=0.30.0
tqdm>=4.0.0
send2trash>=1.8.0
